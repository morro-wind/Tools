#include <stdio.h>

void main()
{
    int n,i=0,m;
    double u[n];
    printf ("Enter n:");
    scanf ("%d",&n);
    if (n>1&&n<=20)
    {
        printf ("n=%d\n",n);
        printf ("Enter i:");
        while((&u[i]=getchar())!=EOF)

            i++;

        for ( m = 0; m<n; m++)
            printf("%lf  ", u[m]);
        return 0;
    }
}
